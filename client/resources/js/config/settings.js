let defaultSettings = {
    'serverHost' : '127.0.0.1',
    'serverPort': 9002,
    'serverProtocol': 'http'
};

let pageSettings = {
    'start' : 1,
    'end': 1,
    'left': 2,
    'right': 2
};

let urlSettings = {
    'login'    : '/api/login',
    'register' : '/api/register'
};

export {defaultSettings, pageSettings, urlSettings};